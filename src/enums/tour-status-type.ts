export enum TourStatusType {
  UPDATED = 'updated',
  DELETED = 'deleted',
  CREATED = 'created',
  SYNCED = 'synced',
  SYNCING = 'syncing' // this status is used to indicate that tour syncing is in progress
}
